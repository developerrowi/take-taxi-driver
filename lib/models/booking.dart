class Booking {
  final String first_name;
  final String last_name;
  final String destination;

  Booking(this.first_name, this.last_name, this.destination);

  // NOTE: implementing functionality here in the next step!
}
